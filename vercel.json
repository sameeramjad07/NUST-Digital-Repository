{
    "rewrites": [
        {
            "source": "${VITE_QALAM_API_NAME}/:path*",
            "destination": "${VITE_QALAM_BASE_URL}/:path*"
        },
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}